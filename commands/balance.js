const { keyv } = require("../keyv.js");

module.exports = {
    name: "balance",
    description: "Checks your balance",
    aliases: ["bal"],
    usage: "<user>",
    execute(message, args) {
        
    }
}